# Backend API base URL (note the /api suffix)
NEXT_PUBLIC_API_URL=http://localhost:5001/api

# Backend Socket.IO server URL (no /api suffix — sockets connect at the root)
NEXT_PUBLIC_SOCKET_URL=http://localhost:5001
